# Emotion-detection-from-text
Final project for the "Deep Learning" class

## Notes:
 - Raport (presentation) is in the root folder
 - The raw data, its preprocessing and the train, val and test datasets are present in the resources/data
 - The source code for the baseline model is in src/baseline
 - The source code for the BERT fine tuning is in src/bert
 - The source code for the custom transformer model (made from scratch) is in src/transformer

## Warning: the best (fine-tuned) BERT model was not uploaded here due to its size (you have to first run train_fine_tune.py before running predictions with it)
